---
const { title } = Astro.props
---

<section class="cv-section section-border" >
  {title && <h2 class="cv-section-h2">{title}</h2>}
  <slot />
</section>

<style>
  .cv-section {
    max-width: 700px;
    margin: 0 auto 48px;
    position: relative;
  }

  .cv-section-h2 {
    margin-bottom: 8px;
    font-weight: 700;
    line-height: 1.5;
    font-size: 2rem;
    color: var(--light-theme-primary-text);
  }

  .section-border {
    outline: 2px dashed transparent;
    outline-offset: 5px;
    transition: 0.5s;
    border-radius: 2px;

    &:hover {
      outline-color: var(--blue);
    }
  }

  @media (width <= 850px) {
    .cv-section {
      max-width: 90%;
    }
  }
</style>
